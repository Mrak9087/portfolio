import React from "react";
import SectionTitle from "../../component/SectionTitle";

const Work = () => {
    return (
        <div className="work">
            <SectionTitle text="Work" />
        </div>
    )
}

export default Work;